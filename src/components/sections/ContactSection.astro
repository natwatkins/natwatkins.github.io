---
import ContactBoxItem from '../ContactBoxItem.astro';
import Section from '../Section.astro';
const { contactOptions } = Astro.props;
---

<Section styles='p-1' ids='contact'>
  <div class='flex flex-col md:flex-row gap-8 md:gap-16 items-center pb-40'>
    <div class='box introduction w-[100%] md:w-[60%]'>
      <h2 class='leading-[4rem] md:leading-[8rem] font-bold text-[4rem] md:text-[6rem]'>
        Get in Touch
      </h2>
    </div>
    <div
      class='flex flex-col info-box w-[100%] md:w-[30%] md:mr-5 gap-5'>
      <div class='flex flex-col p-1'>
        {contactOptions.map((contact) => <ContactBoxItem contact={contact} />)}
      </div>
    </div>
  </div>
</Section>